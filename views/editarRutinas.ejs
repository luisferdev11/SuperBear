<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../css/mastersyle.css" />
    <link rel="stylesheet" href="../../css/formularios.css">
    <link rel="shortcut icon" href="../../assets/svg/logo.svg" />
    <title>Editar Rutina | SuperBear</title>
</head>
<body>
    <!--
        Es necesario definir una variable ruta para que sepa a donde llevar
    -->
    <style>
        nav {
            grid-template-columns: 15%;
        }
    
        nav a img {
            width: 1rem;
            height: 1rem;
        }
    </style>
    <% ruta="/consultarRutinas/" + grupo %>
    <nav class="flecha">
        <a href="<%- ruta %>">
            <img src="../../assets/svg/flecha.svg">
        </a>
    </nav>

    <div class="principal">
        <h1>
            Editar Rutina
        </h1>
    </div>
    
    <div class="contenedor-formulario">
        <form action="/editarRutina/<%= info.id_rut %>/<%= info.id_grp %>" method="post">
            <p>
                Se editará la rutina de la lista:
            </p>
            <b>
                <%= info.id_lis %> - <%= nombre %>
            </b>
            <p>
                La lista se repetirá cada:
            </p>
            <select name="periodo">
                <option <% if(info.prd == 1){ %> selected <%} %> value="1">1 Día</option>
                <option <% if(info.prd == 7){ %> selected <%} %> value="7">7 Días</option>
                <option <% if(info.prd == 15){ %> selected <%} %> value="15">15  Días</option>
                <option <% if(info.prd == 30){ %> selected <%} %> value="30">30  Días</option>
            </select>
            <br><br>
            <div class="contenedor-boton">
                <input class="b" type="submit" value='Editar Rutina'>
            </div>
        </form>
    </div>
    <script src="../../js/detect-browser.js"></script>
    <script src="../../js/barraNavegacion.js"></script>
</body>
</html>